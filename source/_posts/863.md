---
title: 【Nuxt】为 lucide-vue-next 配置自动导入
categories: Nuxt
tags:
  - Nuxt
  - lucide-vue-next
  - 图标
  - 自动导入
sticky: 100
comments: true
toc: true
only:
  - home
  - category
  - tag
date: 2026-3-17 11:37:46
pic:
---


## 安装依赖

```sh
npm i -D unplugin-vue-components
npm i lucide-vue-next
```

## 配置 nuxt.config.ts

```ts
// nuxt.config.ts
import Components from 'unplugin-vue-components/vite'

export default defineNuxtConfig({
  vite: {
    plugins: [
      Components({
        resolvers: [
          (name) => {
            if (name.startsWith('Icon')) {
              return {
                name: name.slice(4),
                from: 'lucide-vue-next'
              }
            }
          }
        ]
      })
    ]
  }
})
```


## 使用

```vue
<template>
  <div>
    <IconAlertCircle />
    <IconActivity />
  </div>
</template>
```

无需 import，Nuxt 自动处理。

## Nuxt 特有坑

❌ 1. SSR hydration mismatch

如果你写：
```vue
<IconHome v-if="process.client" />
```

会导致 SSR 和客户端渲染不一致，报 hydration mismatch 错误。
✅ 解决方法：使用 ClientOnly。

```vue
<ClientOnly>
    <IconHome />
</ClientOnly>
```

❌ 2. 类型提示缺失（TS）
```ts
Components({
  dts: true
})
```

会生成 `components.d.ts`，但默认不包含自动导入的组件。

❌ 3. 图标名大小写

```vue
<Iconhome /> ❌
<IconHome /> ✅
```

## 💡 更“Nuxt 风格”的替代方案
如果你想更 Nuxt 原生一点，可以封装一层：
```vue
<!-- components/Icon.vue -->
<script setup lang="ts">
defineProps<{ name: string }>()
</script>

<template>
  <component :is="name" />
</template>
```

然后：
```vue
<template>
    <Icon name="IconHome" />
    <Icon name="IconAlertCircle" />
</template>
```

👉 但这个方式：
- ❌ 会失去 tree-shaking（不推荐）


